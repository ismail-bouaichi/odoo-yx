<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_payment_schedule_generate_wizard_form" model="ir.ui.view">
            <field name="name">payment.schedule.generate.wizard.form</field>
            <field name="model">payment.schedule.generate.wizard</field>
            <field name="arch" type="xml">
                <form string="Generate Payment Schedule">
                    <group>
                        <group>
                            <field name="vendor_id"/>
                            <field name="partner_id"/>
                            <field name="property_id"/>
                            <field name="final_price"/>
                            <field name="down_payment"/>
                        </group>
                        <group>
                            <field name="generate_mode"/>
                            <field name="installments"/>
                            <field name="first_due_date" invisible="generate_mode != 'by_calendar'"/>
                            <field name="frequency" invisible="generate_mode != 'by_calendar'"/>
                        </group>
                    </group>
                    <footer>
                        <button name="action_generate" type="object" string="Generate" class="btn-primary"/>
                        <button string="Cancel" special="cancel" class="btn-secondary"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_payment_schedule_generate_wizard" model="ir.actions.act_window">
            <field name="name">Generate Schedule</field>
            <field name="res_model">payment.schedule.generate.wizard</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
        </record>
    </data>
</odoo>
