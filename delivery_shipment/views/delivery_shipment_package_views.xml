<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Package List View (for popup) -->
    <record id="view_delivery_shipment_package_list" model="ir.ui.view">
        <field name="name">delivery.shipment.package.list</field>
        <field name="model">delivery.shipment.package</field>
        <field name="arch" type="xml">
            <list string="Packages" create="false" delete="false" edit="false">
                <field name="sequence" widget="handle"/>
                <field name="gab"/>
            </list>
        </field>
    </record>
</odoo>
